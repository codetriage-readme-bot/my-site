{
  "name": "My website",
  "description": "My first full project on Ruby and Rails.",
  "repository": "https://github.com/romasks/blog-sab",
  "success_url": "/",
  "scripts": {
    "postdeploy": "bundle exec rake db:setup"
  },
  "env": {
    "HEROKU_APP_NAME": {
      "required": true
    },
    "HEROKU_PARENT_APP_NAME": {
      "required": true
    }
  },
  "addons": [
    {
      "plan": "heroku-postgresql",
      "options": {
        "version": "9.5"
      }
    }
  ]
}
