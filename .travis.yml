language: ruby

rvm:
  - 2.3.4

before_install:
  - gem uninstall -i /home/travis/.rvm/gems/ruby-2.4.0@global bundler
  - gem install bundler -v 1.15.1
  - bundle install

script:
  - RAILS_ENV=test bundle exec rake db:migrate --trace
  - bundle exec rake db:test:prepare
  - bundle exec bin/rspec spec/controllers

addons:
  code_climate:
    repo_token: a3a36a1f874b79c215786625b036065b02c2b492d1b6b72a43fb4fe3aa293f83
