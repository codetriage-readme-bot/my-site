.col-md-12
  #pics.transitions-enabled
    - @pics.each do |pic|
      .box.panel.panel-default
        = link_to (image_tag pic.image.url), pic
        %h2= link_to pic.title, pic

  - if user_signed_in? and current_user.is_admin
    = link_to 'New One', new_pic_path, class: "btn btn-default"
